# Production build defaults (read by `vite build`).
# LIVE mode (real Cloudflare D1) is the default; users can switch to Demo via
# the in-app toggle. Set to "false" here to make Demo the build default instead.
VITE_USE_API=true
